# Documentation Plan: Deployment & Configuration

I will create a comprehensive documentation file (`DEPLOYMENT.md`) in the root of the repository. This document will serve as a reference for deploying the application to Render (or similar platforms) and managing its configuration.

## Document Structure

1.  **Overview**: Brief description of the deployment architecture (GitHub -> Render, .NET Core + PostgreSQL).
2.  **Prerequisites**: Accounts and tools needed (GitHub, Render, NeonDB).
3.  **Deployment Steps**:
    *   Connecting GitHub Repo to Render.
    *   Setting up the PostgreSQL database (NeonDB).
    *   Configuring Environment Variables.
4.  **Configuration Guide**:
    *   **Environment Variables**: Explain `CONNETION_STRING`, `ASPNETCORE_ENVIRONMENT`, etc.
    *   **Connection String Format**: Explicitly document the supported formats (ADO.NET vs. `postgres://` URI) and the logic we implemented in `DependencyInjection.cs`.
5.  **Troubleshooting**:
    *   Common errors (Connection Refused, Invalid Port).
    *   How to check logs on Render.
    *   The fallback logic for `localhost`.
6.  **Local Development**: How to run the app locally with Docker or `dotnet run`.

I will generate this file immediately.
