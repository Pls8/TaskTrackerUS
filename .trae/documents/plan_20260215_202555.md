I will redesign the dashboard to match the provided image as closely as possible, using the existing data structures and extending them where necessary.

### **Backend Changes**

1.  **Update `TaskDto`**:
    *   Add `CreatedAt` and `LastModifiedAt` properties to support the "Recent Activity" feed.
    *   (These will be automatically mapped from the `ProjectTask` entity).

2.  **Update `DashboardMetricsDto`**:
    *   Add lists to hold the data for the new sections:
        *   `RecentActivities`: A list of the most recently modified or created tasks.
        *   `CurrentProjects`: A list of active high-level tasks (Sections) to display in the "Current Projects" card.
        *   `UpcomingDeadlines`: A list of tasks due soon for the bottom table.

3.  **Update `GetDashboardMetricsHandler`**:
    *   Inject `IMapper` to convert `ProjectTask` entities to `TaskDto`.
    *   Populate the new lists:
        *   **Recent Activities**: Top 5 tasks sorted by `LastModifiedAt` or `CreatedAt` descending.
        *   **Current Projects**: Top 4 active Sections (Parent tasks).
        *   **Upcoming Deadlines**: Top 5 incomplete tasks sorted by `EndDate` ascending.

### **Frontend Changes (The Redesign)**

4.  **Rewrite `Views/Dashboard/Index.cshtml`**:
    *   **Layout**: Implement a 3-column-like structure using Bootstrap Grid (Main Content + Right Sidebar). The global Left Sidebar will remain as the app's main navigation, but I will style the dashboard content to match the clean, card-based aesthetic of the image.
    *   **Top Row**: Create 3 summary cards for **Total Tasks**, **Completion Rate**, and **Overdue**.
    *   **Middle Row**:
        *   **Task Status Chart**: A semi-circle doughnut chart using `Chart.js` (showing Success vs. Pending).
        *   **Current Projects**: A list of active sections with progress badges.
    *   **Bottom Row**: **Upcoming Deadlines** table with columns for Task Name, Priority, Owner, Due Date, and Actions.
    *   **Right Sidebar**: A "Recent Activity" feed showing the latest task updates (Created/Modified) with timestamps (e.g., "2 hours ago").

### **Verification**
*   I will run the application and verify that the dashboard loads with the new design and correct data.
